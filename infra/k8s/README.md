# SaaS Deployment Notes

- Deploy backend as HPA-enabled FastAPI service with GPU node pool for AI workloads.
- Deploy websocket gateway separately for lab/voice low latency streams.
- Use managed PostgreSQL + Redis + object storage.
- Run offline colleges with docker compose and license activation endpoint.
